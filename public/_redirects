# Static assets - serve directly (highest priority)
/assets/*  /assets/:splat  200

# API routes (if any) - adjust based on your API structure
/api/*  /api/:splat  200

# Vite dev server assets
/.vite/*  /.vite/:splat  200

# All other routes - SPA fallback (lowest priority)
/*    /index.html   200